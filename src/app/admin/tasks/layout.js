'use client';

import DashboardLayout from '@/components/DashboardLayout';

export default function AdminTasksLayout({ children }) {
  return (
    <DashboardLayout>
      {children}
    </DashboardLayout>
  );
}
